import SwiftUI

@main
struct ListApp: App {
    var body: some Scene {
        Window("List 列表", id: "main") {
            ContentView()
        }
        .defaultSize(width: 500, height: 500)
    }
}
